using UnityEngine;

/// <summary>
/// Legacy type name kept so existing scene/prefab references resolve after script moves.
/// Prefer renaming references to <see cref="ElectricityConsumptionManager"/> in the editor when convenient.
/// </summary>
public class AetherConsumptionManager : ElectricityConsumptionManager
{
}
